{
  "id": "macro-deck-surface",
  "name": "Macro Deck Surface",
  "version": "1.0.1",
  "description": "Turn the panel into a Macro Deck surface: renders a Macro Deck host's live button page and sends taps, long-presses and releases back over its client WebSocket.",
  "entry": "index.html",
  "served": true,
  "options": [
    {
      "key": "host",
      "label": "Macro Deck host",
      "type": "text",
      "default": "127.0.0.1:8191",
      "help": "Address of the Macro Deck host, e.g. 127.0.0.1:8191 or 192.168.1.50:8191. Macro Deck must be running; the first time you connect, accept this device on the host."
    },
    {
      "key": "clientId",
      "label": "Device name",
      "type": "text",
      "default": "Bedrock Panel",
      "help": "Name this surface shows as on the Macro Deck host when you accept it."
    },
    {
      "key": "layoutMode",
      "label": "Key shape",
      "type": "select",
      "default": "mirror",
      "choices": [["mirror", "Square keys (host layout)"], ["wide", "Wide rectangular keys"]],
      "help": "Square keys match the Macro Deck host and are recommended. Wide changes key shape to fill the panel width; artwork is not stretched."
    },
    {
      "key": "longPressMs",
      "label": "Long-press delay (ms)",
      "type": "number",
      "min": 100,
      "max": 10000,
      "step": 1,
      "default": "1000",
      "advanced": true,
      "help": "How long a key must be held before a long-press action is sent (100–10000)."
    }
  ]
}
